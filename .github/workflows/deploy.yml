name: test windows

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: windows-2019

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
      - run: yarn
      - run: yarn rsw:deploy